function add(a, b) {
    return a + b;
}

function sub(a, b) {
    return a - b;
}

// module.exports = "Chirag";

module.exports = {
    add, sub
}

// export const addFn = add;
// export const subFn = sub;

// exports.add = (a,b) => a + b;
// exports.sub = (a,b) => a - b;
